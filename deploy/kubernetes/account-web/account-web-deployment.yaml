apiVersion: apps/v1
kind: Deployment
metadata:
  name: account-web
  namespace: netmetric
  labels:
    app.kubernetes.io/name: account-web
    app.kubernetes.io/part-of: netmetric
spec:
  replicas: 2
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app.kubernetes.io/name: account-web
  template:
    metadata:
      labels:
        app.kubernetes.io/name: account-web
        app.kubernetes.io/part-of: netmetric
    spec:
      serviceAccountName: account-web
      containers:
        - name: account-web
          image: ghcr.io/netmetric/account-web:REPLACE_WITH_GIT_SHA
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 7004
          envFrom:
            - configMapRef:
                name: account-web-env
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 20
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
