apiVersion: apps/v1
kind: Deployment
metadata:
  name: public-web
  namespace: netmetric
  labels:
    app.kubernetes.io/name: public-web
    app.kubernetes.io/part-of: netmetric
spec:
  replicas: 2
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app.kubernetes.io/name: public-web
  template:
    metadata:
      labels:
        app.kubernetes.io/name: public-web
        app.kubernetes.io/part-of: netmetric
    spec:
      serviceAccountName: public-web
      containers:
        - name: public-web
          image: ghcr.io/netmetric/public-web:REPLACE_WITH_GIT_SHA
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 7003
          envFrom:
            - configMapRef:
                name: public-web-env
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 20
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
