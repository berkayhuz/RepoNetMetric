apiVersion: apps/v1
kind: Deployment
metadata:
  name: tools-web
  namespace: netmetric
  labels:
    app.kubernetes.io/name: tools-web
    app.kubernetes.io/part-of: netmetric
spec:
  replicas: 2
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app.kubernetes.io/name: tools-web
  template:
    metadata:
      labels:
        app.kubernetes.io/name: tools-web
        app.kubernetes.io/part-of: netmetric
    spec:
      serviceAccountName: tools-web
      containers:
        - name: tools-web
          image: ghcr.io/netmetric/tools-web:REPLACE_WITH_GIT_SHA
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 7005
          envFrom:
            - configMapRef:
                name: tools-web-env
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 20
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
